{
  "name": "tesla-to-elasticsearch",
  "version": "0.0.1",
  "description": "collects data from your car and stores in elasticsearch",
  "main": "index.js",
  "scripts": {
    "deploy:prod": "docker-compose -f docker-compose-deploy.yml up --build -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mattias Rost",
  "license": "ISC",
  "dependencies": {
    "agentkeepalive": "^3.3.0",
    "elasticsearch": "^14.0.0",
    "json-bigint": "^0.2.3",
    "nconf": "^0.10.0",
    "request": "^2.83.0",
    "request-promise-native": "^1.0.5"
  },
  "devDependencies": {
    "semistandard": "^12.0.0"
  }
}
